import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
};


module.exports = {
  images: {
    domains: ['pbs.twimg.com',  'images.unsplash.com', 'assets.aceternity.com'], // Add the hostname here
  },
  // ... other configurations
}
export default nextConfig;
